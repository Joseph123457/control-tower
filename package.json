{
  "name": "control-tower",
  "version": "1.0.0",
  "description": "Claude Code CLI를 GUI 대시보드로 제어하는 웹 앱",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "build:all": "npm run install:all && npm run build",
    "start": "set NODE_ENV=production&& cd backend && npm start",
    "start:unix": "NODE_ENV=production cd backend && npm start",
    "test": "cd backend && npm test",
    "clean": "rimraf frontend/dist backend/node_modules frontend/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
